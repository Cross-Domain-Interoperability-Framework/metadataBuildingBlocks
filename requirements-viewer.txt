# Runtime dependencies for tools/cdif_viewer_app.py only.
#
# Deliberately separate from requirements.txt, which covers the whole toolchain:
# the viewer needs neither jsonschema (it does not validate) nor pyshacl (which
# pulls rdflib, owlrl and more). Installing those on the hosted deployment would
# add minutes to every build and a lot of surface for no benefit.
#
# Everything else the viewer uses is standard library.

PyYAML>=6.0
