{
  "@context": {
    "schema": "http://schema.org/",
    "dcterms": "http://purl.org/dc/terms/",
    "ex": "https://example.org/"
  },
  "@type": [
    "schema:DataDownload"
  ],
  "schema:name": "Query result (CSV)",
  "schema:description": "Per-request CSV representation returned by the dataset query API. The response is generated per request, so it carries no contentUrl or contentSize.",
  "schema:encodingFormat": [
    "text/csv"
  ],
  "dcterms:conformsTo": [
    {
      "@id": "https://example.org/schema/query-result"
    }
  ]
}
