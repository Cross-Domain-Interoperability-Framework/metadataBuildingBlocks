- title: Example organization.
  content: |-
    Example organization instance.
  prefixes:
    schema: http://schema.org/
  snippets:
    - language: json
      ref: exampleOrg.json

- title: Complete organization example.
  content: |-
    Organization instance exercising all properties: additionalType (multiple),
    name, alternateName, description, identifier (ROR), and sameAs (string and object).
  prefixes:
    schema: http://schema.org/
  snippets:
    - language: json
      ref: exampleOrganizationComplete.json

