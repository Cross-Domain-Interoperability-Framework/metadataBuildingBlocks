- title: Link role building block.
  content: |-
    A typed link whose relationship is given as a plain string label.
  prefixes:
    schema: http://schema.org/
  snippets:
    - language: json
      ref: exampleLinkRole.json

- title: Complete link role example.
  content: |-
    The relationship given as a SKOS concept rather than a string, with a fully
    described EntryPoint target.
  prefixes:
    schema: http://schema.org/
    skos: http://www.w3.org/2004/02/skos/core#
  snippets:
    - language: json
      ref: exampleLinkRoleComplete.json
