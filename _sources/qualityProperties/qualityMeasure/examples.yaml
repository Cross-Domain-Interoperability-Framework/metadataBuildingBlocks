- title: Example quality measure.
  content: |-
    Example quality measure
  prefixes:
    schema: http://schema.org/
  snippets:
    - language: json
      ref: exampleQualityMeasure.json

- title: Quality measure with a numeric result.
  content: |-
    A quality measure whose result is a number rather than a string or a Defined Term — here the
    positional accuracy of the example above, reported as a distance instead of a pass/fail term.
  prefixes:
    schema: http://schema.org/
  snippets:
    - language: json
      ref: exampleQualityMeasureNumeric.json
