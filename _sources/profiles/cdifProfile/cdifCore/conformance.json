{
  "$schema": "https://w3id.org/cdif/schema/conformance-declaration/0.1",
  "conformsTo": "https://w3id.org/cdif/core/1.1",
  "title": "CDIF Core profile",
  "description": "The baseline described-dataset class: a schema:Dataset (not the catalog record) carrying a name and an identifier. Structural validity is the JSON Schema's job, so there is no content SHACL gate.",
  "presence": "ASK {\n  ?d a schema:Dataset ; schema:name ?n ; schema:identifier ?i .\n  FILTER NOT EXISTS { ?d schema:additionalType dcat:CatalogRecord }\n}",
  "validityShapes": null
}
