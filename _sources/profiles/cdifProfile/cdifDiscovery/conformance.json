{
  "$schema": "https://w3id.org/cdif/schema/conformance-declaration/0.1",
  "conformsTo": "https://w3id.org/cdif/discovery/1.1",
  "title": "CDIF Discovery profile",
  "description": "The baseline discoverable-dataset profile. Any core record is discovery-conformant (the module adds optional enrichment, no new requirements), so the presence test is the same as core and it is declared alongside core. cdifDiscovery is a thin wrapper with no content SHACL.",
  "presence": "ASK {\n  ?d a schema:Dataset ; schema:name ?n ; schema:identifier ?i .\n  FILTER NOT EXISTS { ?d schema:additionalType dcat:CatalogRecord }\n}",
  "validityShapes": null
}
