---
$schema: https://json-schema.org/draft/2020-12/schema
title: CDIF Concept Scheme Profile (archived)
description: ARCHIVED placeholder. Concept-scheme metadata is not promoted to a
  composite CDIF profile. Use cdifConceptScheme (cdifProfile) as a module within
  other composite profiles instead.
allOf:
- $ref: ../../../profiles/cdifProfile/cdifConceptScheme/schema.yaml
