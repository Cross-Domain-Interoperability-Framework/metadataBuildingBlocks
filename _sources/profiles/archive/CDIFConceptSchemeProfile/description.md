# CDIF Concept Scheme Profile (archived)

**Status: archived / not a composite profile.**

Concept-scheme metadata is not promoted to a standalone composite CDIF
profile. Use the `cdifConceptScheme` building block from `cdifProfile/` as a
module within other composite profiles instead.

Retained in the archive folder as a register entry for traceability.
