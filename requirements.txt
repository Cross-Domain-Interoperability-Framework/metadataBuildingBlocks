# Python dependencies for the CDIF building-blocks tooling.
#
#   resolve_schema.py / regenerate_schema_json.py
#       Standard library only (json, os, sys, copy, argparse, pathlib, ...).
#
#   validate_examples.py  (default JSON Schema validation gate)
#       jsonschema, PyYAML
#
#   validate_shacl.py     (optional, standalone SHACL validation)
#       pyshacl  (pulls in rdflib, which also provides the JSON-LD parser)
#
# Minimum Python version: 3.8+

jsonschema>=4.18
PyYAML>=6.0

# Optional: only needed to run tools/validate_shacl.py
pyshacl>=0.31
